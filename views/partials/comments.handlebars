<div class="comments box m-3">
  {{#each this}}
  <section class="comment message">
    <div class="text message-body">
      {{comment_text}}
    </div>
    <div class="meta message-header">
      <p>
        <strong>
          {{user.username}}
        </strong>
        on {{format_date created_at}}
      </p>
    </div>

    {{!-- <a>Edit</a>
    <a>Delete</a> --}}


  </section>
  {{/each}}
</div>